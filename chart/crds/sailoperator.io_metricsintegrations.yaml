---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: metricsintegrations.sailoperator.io
spec:
  group: sailoperator.io
  names:
    categories:
    - istio-io
    kind: MetricsIntegration
    listKind: MetricsIntegrationList
    plural: metricsintegrations
    singular: metricsintegration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Metrics integration type.
      jsonPath: .spec.type
      name: Type
      type: string
    - description: Whether reconciliation succeeded.
      jsonPath: .status.conditions[?(@.type=="Reconciled")].status
      name: Reconciled
      type: string
    - description: The age of the object.
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MetricsIntegration configures metrics observability integrations
          for Istio.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MetricsIntegrationSpec defines the desired state of MetricsIntegration.
            properties:
              clusterObservabilityOperator:
                description: |-
                  ClusterObservabilityOperator configures integration with the Cluster Observability
                  Operator's MonitoringStack resource for metrics collection.
                properties:
                  monitoringStackRef:
                    description: |-
                      MonitoringStackRef is a reference to a MonitoringStack resource that defines
                      the Prometheus stack used for scraping Istio metrics.
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                required:
                - monitoringStackRef
                type: object
              targetRefs:
                description: TargetRefs specifies the resources that this integration
                  configures.
                items:
                  description: TargetReference identifies a resource that an Integration
                    configures.
                  properties:
                    kind:
                      description: 'Kind specifies the target kind: "Istio", "Kiali",
                        or "PersesDatasource".'
                      enum:
                      - Istio
                      - Kiali
                      - PersesDatasource
                      type: string
                    name:
                      description: Name is the name of the target resource.
                      type: string
                    namespace:
                      description: |-
                        Namespace is the namespace of the target resource.
                        Required for namespace-scoped resources like Kiali and PersesDatasource.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                minItems: 1
                type: array
              type:
                description: Type specifies the metrics integration type.
                enum:
                - UserWorkloadMonitoring
                - ClusterObservabilityOperator
                type: string
              userWorkloadMonitoring:
                description: UserWorkloadMonitoring configures integration with OpenShift
                  User Workload Monitoring.
                type: object
            required:
            - targetRefs
            - type
            type: object
          status:
            description: MetricsIntegrationStatus defines the observed state of MetricsIntegration.
            properties:
              conditions:
                items:
                  description: StatusCondition represents a specific observation of
                    an object's state.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        the last transition.
                      type: string
                    reason:
                      description: Unique, single-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: The status of this condition. Can be True, False
                        or Unknown.
                      type: string
                    type:
                      description: The type of this condition.
                      type: string
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
